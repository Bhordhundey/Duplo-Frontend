import { makeStyles, Theme } from "@material-ui/core";
import { createStyles } from "@material-ui/styles";


const ListShimmerStyles = makeStyles((theme: Theme) =>
    createStyles({
        root:{
            alignContent:'center'
        }
    }),
);
export default ListShimmerStyles